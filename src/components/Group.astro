---
const { data, slug } = Astro.props
---

<article>
	<div class="p-4 bg-white rounded-lg ring-1 ring-gray-100">
		<div class="flex flex-col gap-3">
			<div class="flex items-center justify-between gap-2 flex-wrap">
				<h4 class="font-bold text-lg">"{data.title}"</h4>
				{
					data.type === "Онлайн" ? (
						<span class="bg-green-100 ring-1 ring-green-200 px-2 py-1 rounded-md">
							🌐 {data.type}
						</span>
					) : (
						<span class="bg-sky-100 ring-1 ring-sky-200 px-2 py-1 rounded-md">
							🌤 {data.type}
						</span>
					)
				}
			</div>

			<div class="mt-8 flex flex-col gap-4">
				<time class="" datetime={data.dateTime}>
					<span
						>{data.when}:
						<span class="font-bold text-emerald-500">{data.dateTime}</span></span
					>
				</time>
				<div class="flex flex-wrap gap-4">
					<a
						href={`/groups/${slug}`}
						class="py-1 px-3 rounded-md bg-blue-500 text-white hover:bg-blue-700"
					>
						Подробнее о группе
					</a>
					<a
						href={data.link}
						class="py-1 px-3 font-medium rounded-md bg-emerald-500 text-white hover:bg-emerald-700"
					>
						Войти в чат
					</a>
				</div>
			</div>
		</div>
	</div>
</article>
